<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pt.ulisboa.tecnico.cmov.locmess">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LogInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignUpActivity" />
        <activity android:name=".MainMenuActivity" />
        <activity
            android:name=".MainProfileActivity"
            android:label="@string/title_activity_main_profile"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ViewKeyActivity" />
        <activity
            android:name=".locations.MainLocationsActivity"
            android:label="@string/title_activity_main_locations"/>
        <activity android:name=".locations.ViewLocationActivity" />
        <activity android:name=".locations.AddLocationActivity" />
        <activity
            android:name=".MainMessagesActivity"
            android:label="@string/title_activity_main_locations"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".AddMessageActivity" />
        <activity android:name=".ViewMessageActivity" />
        <activity android:name=".AddMessageSecondActivity"></activity>
    </application>

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-feature android:name="android.hardware.location.gps" />

</manifest>